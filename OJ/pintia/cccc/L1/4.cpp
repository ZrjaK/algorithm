#include <bits/stdc++.h>
#define long long ll;
using namespace std;

const int N = 1e4 + 10;

signed main() {
    int n;
    cin >> n;
    cout << "Celsius = " << 5 * (n - 32) / 9 << endl;
    return 0;
}